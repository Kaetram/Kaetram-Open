import Default from './default';

import Mob from '@kaetram/server/src/game/entity/character/mob/mob';

export default class Snek extends Default {
    public constructor(mob: Mob) {
        super(mob);
    }
}
